#

##

result.push(nums[i], nums[n+i])

### https://leetcode.com/problems/the-k-strongest-values-in-an-array/

#### question

`let result2: number = []` what's wrong with this?
answer
`result2: number` is saying result2 is a number type but [] is an array type.

solution
`let result = [];`
`let result3: any = [];`

#### Question

`result3.push(nums[i], nums[i+n])`
answer
it's the same as
`result3.push(nums[i]`
`result3.push(nums[i+n]`

    const result =[]
    for(let i=0;i<n;i++){
        result.push(nums[i], nums[n+i])
    }
    return result

### https://leetcode.com/problems/the-k-strongest-values-in-an-array/

1. array.sort

- array sort using array.sort(a,b)=>a-b)
- a is the first element, b is the second element
- a-b is the ascending order
- b-a is descending order

`array.sort(comparator(a,b) {return a-b})` || `array.sort((a,b)=>a-b)`

2. use Math.abs() to get the absolute value

3. use array.slice to return the first k elements `arr.slice(0, k)`

4. use parseInt() to get the integer value of a number
   `parseInt(5.5) // 5`

not working

```js
array: number[] = [1,2,3,4,5]
const median = array[parseInt(array.length-1/2)] **error - cannot parse number as string**
```

working

```js
const tempLen: any = array.length - 1 / 2;
const median: any = array[parseInt(tempLen)];
```
