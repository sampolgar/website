Logging with express.js
app.use(express.urlencoded({ extended: true })) // for parsing application/x-www-form-urlencoded
app.use(express.json({extended: true}));
getting query string params
const query = req.query; console.log(JSON.stringify(req.query));

logging headers
const headers = req.headers console.log(JSON.stringify(headers))

logging request url
console.log("req.url ", JSON.stringify(req.originalUrl))

logging post form data
console.log("body: "+JSON.stringify(req.body));
